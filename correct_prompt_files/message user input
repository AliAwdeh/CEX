Evaluate the target agent message using the conversation history and metadata below.

You are judging only the target agent message, but you must use the full visible history up to that message to understand customer intent, context, repetition, frustration, and journey progress.

The conversation history includes the target message as the final message.

Return strict JSON only using the required schema.

Input:
{payload_json}

